'use client';

import { WorkoutGenerationNotification } from './WorkoutGenerationNotification';

export function WorkoutGenerationNotificationWrapper() {
  return <WorkoutGenerationNotification />;
}

